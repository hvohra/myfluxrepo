apiVersion: helm.toolkit.fluxcd.io/v2beta1 # Or your current apiVersion
kind: HelmRelease
metadata:
  name: demo6-nginx-hr
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: nginx # Just the chart name, not the full OCI path
      version: 21.1.4
      sourceRef:
        kind: HelmRepository
        name: bitnamicharts
        namespace: flux-system

